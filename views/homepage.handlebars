{{#each posts as |post| }}
<div class="card">
    <div class="header">
        {{post.title}} {{post.date_created}}
    </div>
    <div class="main">
        {{post.description}}
    </div>
</div>
{{/each}}