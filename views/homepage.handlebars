{{#each posts as |post|}}
  <div class="card">
    <h2>{{post.title}}</h2>
    <p>{{format_date post.date_created}}</p>
    <p>{{user.username}}</p>
    <p>{{post.content}}</p>
    <a href="/post/{{this.id}}"><button>Go to post</button></a>
  </div>
{{/each}}